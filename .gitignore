# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local config / secrets
.env

# Vector store
.chroma/

# Raw run logs (regenerated by `make attack`)
results/runs/*.jsonl
# Note: results/report.csv and results/charts/*.png are committed as sample
# artifacts so the README renders; regenerate them with `make attack`.

# Canary registry (regenerated)
data/corpus/.canary_registry.json

# OS
.DS_Store
Thumbs.db
